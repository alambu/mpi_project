$('#fromvalid').bootstrapValidator();

        
$('.default-date-picker').datepicker({
    format: 'mm-dd-yyyy',
    autoclose: true
});

              $('#example').dataTable( {
                  "aaSorting": [[ 4, "desc" ]]
              } );
